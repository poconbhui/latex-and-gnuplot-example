#
# Load all the necessary files.
#
# This include file is handy for adding/removing a bunch of stuff and
# only having to include this file in all the other plot files.
#
# Plot files can override anything set here by setting their own parameters,
# eg term or output file (handy for debugging) after loading this file.
#
load "gnuplot/output.gnu"
load "gnuplot/styles.gnu"
